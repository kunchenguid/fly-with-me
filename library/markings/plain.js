// Plain: the coloring alone. First in the registry, so it is the marking a
// kind in its own colors wears; it paints nothing.
import { defineMarking } from '../contract.js';

export default defineMarking({ id: 'plain', about: 'the coloring alone' });
